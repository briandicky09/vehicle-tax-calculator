#ifndef DETAIL_H
#define DETAIL_H
#include <QWidget>
#include <QLabel>
#include <QLineEdit>
#include <QTextEdit>
#include <QComboBox>
#include <QPushButton>
#include <QStackedWidget>
#include "Datakendaraan.h"

class Detail : public QWidget
{
    Q_OBJECT

public:
    explicit Detail(QWidget *parent = nullptr);

    void tampilkan(const DataKendaraan &data, int index);

signals:
    void kembaliDiminta();
    void hapusDiminta(int index);
    void editDisimpan(int index, DataKendaraan data);

private slots:
    void onEdit();
    void onHapus();
    void onSimpanEdit();
    void onBatalEdit();

private:
    void buildUI();
    void isiViewMode(const DataKendaraan &d);
    void isiEditMode(const DataKendaraan &d);

    int           currentIndex = -1;
    DataKendaraan currentData;

    //simpan
    QWidget *viewWidget  = nullptr;
    QLabel  *vNama       = nullptr;
    QLabel  *vAlamat     = nullptr;
    QLabel  *vNomor      = nullptr;
    QLabel  *vJenis      = nullptr;
    QLabel  *vTahun      = nullptr;
    QLabel  *vNJKB       = nullptr;
    QLabel  *vPKB        = nullptr;
    QLabel  *vSWD        = nullptr;
    QLabel  *vTotal      = nullptr;
    QLabel  *vTanggal    = nullptr;
    QPushButton *btnEdit = nullptr;
    QPushButton *btnHapus= nullptr;

    //edit
    QWidget     *editWidget  = nullptr;
    QLineEdit   *eNama       = nullptr;
    QTextEdit   *eAlamat     = nullptr;
    QLineEdit   *eNomor      = nullptr;
    QComboBox   *eJenis      = nullptr;
    QLineEdit   *eTahun      = nullptr;
    QLineEdit   *eNJKB       = nullptr;
    QPushButton *btnSimpan   = nullptr;
    QPushButton *btnBatal    = nullptr;

    QStackedWidget *modeStack = nullptr;
};

#endif // DETAIL_H