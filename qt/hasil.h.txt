#ifndef HASIL_H
#define HASIL_H
#include <QWidget>
#include <QLabel>
#include <QPushButton>
#include "Datakendaraan.h"

class Hasil : public QWidget
{
    Q_OBJECT

public:
    explicit Hasil(QWidget *parent = nullptr);
    void tampilkanData(const DataKendaraan &data, const QString &pkbStr,
                       const QString &swdStr, const QString &totalStr);

signals:
    void kembaliDiminta();
    void simpanDiminta();

private:
    void buildUI();

    QLabel *lblNama   = nullptr;
    QLabel *lblNomor  = nullptr;
    QLabel *lblJenis  = nullptr;
    QLabel *lblNJKB   = nullptr;

    QLabel *lblPKB    = nullptr;
    QLabel *lblSWD    = nullptr;
    QLabel *lblTotal  = nullptr;

    QPushButton *btnKembali = nullptr;
    QPushButton *btnSimpan  = nullptr;
};

#endif // HASIL_H